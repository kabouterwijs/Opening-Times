%h1= @page_title = "Users"

%p
  Opening Times has
  = @user_count
  registered users. I'll provide some better statistics soon :)

%h2 Top users

%h3 New entries

%ol
  - @leaderboard_new.each do |user|
    %li
      = link_to "User #{user[0]}", user_path(user[0])
      - 
      = pluralize(user[1], "edits")
    
  
%h3 Edits

%ol
  - @leaderboard_edits.each do |user|
    %li
      = link_to "User #{user[0]}", user_path(user[0])
      - 
      = pluralize(user[1], "edits")
    


